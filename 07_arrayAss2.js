const arrayNumber=[20,31,40,25,23,11,29,9,60,2,11];
console.log(`Total Number of Array is:${arrayNumber}`);
console.log(`Total length of Array:${arrayNumber.length}`);

let firstElement = arrayNumber[0]
let lastElement = arrayNumber[arrayNumber.length-1]
console.log(`First and last element of provided array is:`);
console.log(`first element of provied array is:${firstElement}`);
console.log(`Last element of provied array is:${lastElement}`);

let thirdElement = arrayNumber[arrayNumber.length-3]
console.log(`third last element in provied array is:${thirdElement}`);






